# Contributing

Use stable Rust. Run the CI commands locally before submitting a pull request. Keep domain types independent of transports; accompany behavior changes with contract tests. Use conventional commits. APIs are unstable during 0.1 development.
